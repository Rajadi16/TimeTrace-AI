{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile:timetrace-ai",
      "type": "shell",
      "command": "npm run compile",
      "options": {
        "cwd": "${workspaceFolder}/timetrace-ai"
      },
      "problemMatcher": [
        "$tsc"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
