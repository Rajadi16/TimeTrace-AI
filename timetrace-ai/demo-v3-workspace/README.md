# TimeTrace AI V3 Demo Workspace

This folder is a minimal TypeScript app designed to demo TimeTrace AI V3 features reliably.

Dependency flow:

controller -> service -> api -> cache -> db

Use `DEMO_SCRIPT.md` for exact live-edit steps and expected extension behavior.
